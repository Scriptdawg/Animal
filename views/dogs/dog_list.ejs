<h1><%= pageTitle %></h1>
<p>
  To test database connectivity, these dogs are retrieved from a mongoDb cloud
  database called animals and a collection of documents called dogs.
</p>

<ul>
  <% dogs.forEach((dog) => { %>
      <a href="dog/<%= dog.url %>"><h2><%= dog.name %></h2>
        <% if(dog.coverImagePath) { %>
          <img width="500" height="300" src="<%= dog.coverImagePath %>" />
          <% } %>
        </a>
  <% }); %>
</ul>

<p><a href="/animal/dog/create">Create New Dog</a></p>
